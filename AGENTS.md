# AGENTS.md

このファイルは、Chrome拡張機能「がぞうみまもり｜Xセンシティブ画像フィルター」で作業するAIコーディングエージェント向けの開発ガイドです。作業前に本内容を確認し、既存のプライバシー方針・多言語方針・Chrome Web Store 審査観点と矛盾しないように進めてください。

## プロジェクト概要

- 対象リポジトリ: `na0AaooQ/nsfw-guardian-image-hideorshow`
- X（旧Twitter）のタイムライン、検索結果、DMなどに表示される画像を、ブラウザ内のAI判定でセンシティブ画像として検出し、必要に応じてブロック表示に差し替えるChrome拡張機能です。
- NSFWJS / TensorFlow.js を利用した画像判定は、拡張機能内で完結する設計を維持します。
- ユーザーはポップアップUIから、有効/無効、判定感度、表示言語を設定できます。

## 最重要方針: プライバシーと安全性

この拡張機能では、以下の情報を外部送信しない方針を最重要として扱ってください。

- 画像データ
- 判定結果
- スコア
- 閲覧URL
- Xの投稿内容
- DM内容
- Xアカウント情報
- `language` などの設定値

画像判定はブラウザ内で完結させ、外部APIや外部サーバーへの送信を前提にした実装は避けてください。仕様変更が必要な場合は、プライバシーポリシー、Chrome Web Store 掲載内容、ユーザーへの説明に影響がないかを必ず確認してください。

## Chrome拡張機能としての基本方針

- Manifest V3 の制約に沿って実装してください。
- 外部スクリプトの読み込みや、CSPに反する実装を追加しないでください。
- `permissions` / `host_permissions` は最小限を維持してください。
- Xの画面構造変更に対応する場合も、過剰な権限追加や外部通信に頼らない方針を優先してください。
- 変更がChrome Web Store の審査に影響する可能性がある場合は、提出前に理由と影響範囲を整理してください。

## 権限追加・変更時の確認

`manifest.json` の `permissions` / `host_permissions` を追加・変更する場合は、事前に以下を確認してください。

- なぜその権限が必要か
- 既存権限で代替できないか
- プライバシーポリシー更新が必要か
- Chrome Web Store 審査への影響があるか
- ユーザーに追加説明が必要か

不要な権限追加は避け、必要な場合も理由を明確にしてください。

## 各ファイル・領域の役割

### `popup.html` / `popup.js`

- 拡張機能ポップアップUIを担当します。
- 有効/無効、感度、表示言語設定を扱います。
- `language` の設定値は `auto` / `ja` / `en` を想定します。

### `content.js`

- Xページ上の画像検出を担当します。
- 画像判定の依頼、判定結果に応じたブロック表示への差し替えを行います。
- 画像ブロック時の文言表示は、ユーザーの表示言語設定を反映してください。

### `offscreen.html` / `offscreen.js`

- ブラウザ内AI画像判定を担当します。
- NSFWJS / TensorFlow.js 関連の初期化、モデル読み込み、推論処理を扱います。
- 外部APIに画像や判定結果を送信しない設計を維持してください。

### `styles.css`

- Xページ上に挿入するブロック表示の見た目を担当します。
- X本体のUIを大きく壊さないよう、クラス名やスタイルの影響範囲に注意してください。

### `docs/`

- 操作マニュアル、プライバシーポリシー、多言語ドキュメントを管理します。
- UI文言、操作手順、権限、保存情報、処理情報に変更がある場合は、更新要否を確認してください。

### `_locales/`

- `manifest.json` 用のChrome標準 i18n メッセージを管理します。
- `manifest.json` の `__MSG_*__` と `_locales/*/messages.json` の整合性を確認してください。

## 多言語対応方針

- `manifest.json` はChrome標準 i18n を使用します。
- `default_locale` は `en` です。
- `_locales/en/messages.json` と `_locales/ja/messages.json` を使用します。
- `popup` / `content` の表示文言は、ユーザーが選択した `language` 設定を反映するため、独自辞書で切り替えます。
- `language` の値は `auto` / `ja` / `en` です。
- `auto` の場合、Chrome UI言語が `ja` または `ja-*` なら日本語、それ以外は英語を表示します。
- 表示文言を変更した場合は、manual の更新要否を確認してください。
- 設定値を変更・追加した場合は、privacy-policy の更新要否を確認してください。
- `docs/` の多言語ページを更新する場合は、既存の言語構成を尊重してください。
- アラビア語ページでは `dir="rtl"` や `.ltr` 指定を壊さないよう注意してください。

## privacy-policy / manual 更新確認が必要な条件

以下に該当する変更では、`docs/` の更新要否を確認してください。

- 保存する設定値を追加・変更した場合
- 取得・処理する情報の範囲が変わる場合
- Chrome権限を追加・変更する場合
- UI文言や操作手順が変わる場合
- 画像ブロック表示の文言が変わる場合
- 対応バージョンが変わる場合
- Chrome Web Store 掲載内容に影響する場合

## `models/tf-wasm-bundle.js` の扱い

- `models/tf-wasm-bundle.js` は README の手順に従って生成が必要です。
- `.gitignore` 対象であり、Git管理しません。
- `offscreen.html` は `models/tf-wasm-bundle.js` を読み込みます。
- `tests/offscreen.html.test.js` により、必要な `script` ファイルが存在するか確認されます。
- `npm test` 実行前に、必要に応じて `models/tf-wasm-bundle.js` の生成状態を確認してください。

## テスト・確認コマンド

変更内容に応じて、以下を実行してください。

```bash
npm test
npm test -- --runInBand
git diff --check
```

必要に応じて、`manifest.json` や `_locales/*.json` がJSONとして正しいことも確認してください。

## Chrome Web Store 提出前チェック

提出前には、少なくとも以下を確認してください。

- `manifest.json` の `version` が正しい
- 新しい権限追加がない、または理由が明確
- `models/tf-wasm-bundle.js` が生成済み
- 不要ファイルをZIPに含めない
- privacy-policy と manual が最新
- README / docs と実装の整合性がある
- 手動読み込みで動作確認済み
- Xのタイムライン、検索結果、DMで動作確認済み
- 外部送信なし方針が維持されている

## 作業時の注意

- 変更は目的に必要な範囲に限定してください。
- AGENTS.md 以外のファイル変更が必要になった場合は、理由を整理してから実施してください。
- 既存の実装、README、docs、Chrome Web Store 掲載内容と矛盾する記載を追加しないようにしてください。
- 断定しきれない仕様変更では、確認観点と影響範囲を明記してください。
